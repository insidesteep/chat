{
  "name": "mern-chat",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "scripts": {
    "start": "SET NODE_ENV=production&& node server/index.js",
    "dev": "SET NODE_ENV=development && concurrently \"npm run server:dev\" \"npm run client:start\"",
    "client:build": "cd client && npm run build",
    "client:start": "cd client && npm run start",
    "server:start": "cd server && npm run start",
    "server:dev": "cd server && npm run server",
    "heroku-postbuild": "npm run client:build"
  },
  "author": " <salaxiddinovsuxrob@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.3.0"
  }
}
